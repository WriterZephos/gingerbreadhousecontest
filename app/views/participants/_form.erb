
<div class="form-div text-content">
  <%= form_with(model: @participant, url: [@user, @participant]) do |form| %>
    <%= form.hidden_field :user_id %>
    <div class="field">
      <%= form.label :name %>
      <%= form.text_field :name, required: true %>
    </div>

    <div class="field">
      <%= form.label :age_at_creation, "Age" %>
      <%= form.number_field :age_at_creation, required: true %>
    </div>

    <div class="actions">
      <%= form.submit "Add Participant" %>
    </div>
  <% end %>
  <p>After your participants are added, you can continue back to the home page by clicking <%= link_to "here", root_path, data: { turbo: "false" } %>. You can always get back to this page by clicking the Account link in the navigation menu at the top of the page.</p>
</div>
